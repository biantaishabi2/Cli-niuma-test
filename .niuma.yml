ai:
  default: stub

  providers:
    stub:
      cmd: "bash .niuma/mock_provider.sh {prompt_file}"
      cmd_agent: "bash .niuma/mock_provider.sh {prompt_file}"

  discussion:
    providers:
      - stub
      - stub

  implementation:
    provider: stub

workflow:
  max_discussion_rounds: 5
  visible_round_interval: 1
  visible_only_on_diff: true

daemon:
  poll_interval: "15s"
  max_concurrent: 1
  tracker: "github"
  github:
    owner: "biantaishabi2"
    project_number: 2
